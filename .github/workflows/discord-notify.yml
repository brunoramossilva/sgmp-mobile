name: Discord Notification

on:
  pull_request:
    types: [opened, reopened, closed]
  push:
    branches:
      - develop
      - main
  create: {}

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send message to Discord
        uses: Ilshidur/action-discord@master
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK_URL }}
        with:
          args: |
            ${{ github.event_name == 'create' && github.ref_type == 'branch' && format('🌱 **Nova branch criada:** `{0}` por @{1}', github.ref_name, github.actor) ||
            github.event_name == 'push' && format('📦 Push na branch `{0}` por @{1}', github.ref_name, github.actor) ||
            github.event_name == 'pull_request' && format('🔀 PR {0} por @{1}', github.event.action, github.actor) ||
            format('⚙️ Evento: {0}', github.event_name) }}
